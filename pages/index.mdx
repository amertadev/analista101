# Pendahuluan
 
analista101 adalah sebuah platform yang menyediakan tutorial, tips, dan trik seputar analisis data. Platform ini dibuat dengan tujuan untuk membantu teman-teman analista apabila sedang mengalami kesulitan dalam menganalisis data.

## Cara Kontribusi Halaman

- Tentukan halaman yang ingin direvisi
- Jika mau melaporkan adanya bug bisa klik tombol ```Question? Give us feedback →``` di kanan layar.
- Jika mau mengedit halaman klik tombol ```Edit this page``` di kanan layar.

## Cara Kontribusi Project

1. Fork repository ini
2. Clone repository yang sudah di-fork
3. Buat branch baru dengan nama yang deskriptif
4. Lakukan perubahan
5. Commit perubahan yang sudah dilakukan
6. Push branch ke repository yang sudah di-fork
7. Buat pull request ke repository ini
8. Tunggu pull request di-review oleh owner repository

## Cara Menjalankan Project

import { Tabs } from 'nextra/components'

Install dependencies

<Tabs items={['pnpm', 'npm', 'bun']}>
  <Tabs.Tab>
    ```bash copy
    pnpm install
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    npm install
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    bun install
    ```
  </Tabs.Tab>
</Tabs>

Jalankan project dan buka localhost:3000
    
<Tabs items={['pnpm', 'npm', 'bun']}>
  <Tabs.Tab>
    ```bash copy
    pnpm run dev
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    npm run dev
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    bun run dev
    ```
  </Tabs.Tab>
</Tabs>